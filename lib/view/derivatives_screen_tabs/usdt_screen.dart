import 'package:flutter/material.dart';


class USDTScreen extends StatelessWidget {
  const USDTScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: double.infinity,

    );
  }
}
